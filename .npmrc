# This prevents pnpm from failing the build if pnpm-lock.yaml is out of sync with package.json
# This is useful in CI environments where the lockfile might not always be perfectly up-to-date
# with the latest package.json changes from different branches or automated updates.
frozen-lockfile=false
